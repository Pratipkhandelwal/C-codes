     #include<iostream>
     #include <vector>
     using namespace std;

    vector <int> adj[100000];

    int main()
    {
        int x, y, n, m,node1, node2;
        cin >> n;       //Number of nodes
        cin >> m;   //No of edges 
		        for(int j=0;j<nodes;j++)
        {
            cin >> arr[j];
        }
        for(int i = 0;i < edges;++i)
        {
                cin >> x >> y;
            adj[x].push_back(y);
        }
         for(int j=0;j<m;j++)
        {
        	
		}
         if(!ans)
        cout << "NO" << endl;
    }
    return 0;
    }

